# ashare-monitor 命令参考

> 自动生成于 2026-08-24 20:38（scripts/gen_docs.py），勿手改
> 共 53 个命令；命令行 `--help` 与本文档始终一致

## 命令总览

### 体检与分析

- `analyze` — 拉取个股历史数据并分析
- `advice` — 规则化交易信号（结合实时行情）
- `indicator` — 技术指标（MACD/RSI/KDJ/BOLL）
- `backtest` — 持有期回测（买入日/金额/持有交易日数）
- `verify` — 信号命中率验证（基于回填 K 线回测）
- `timing` — 择时买入提醒（收盘后扫描买点信号）
- `doctor` — 个股全方位体检（评分+汇总报告）
- `profile` — 公司档案（工商信息 + 股权结构）
- `check` — 标的资料完整性体检（股票/港股/数字货币）
### 其他

- `valuation` — 估值分位（PE/PB 历史百分位）
- `sector` — 月度产销快报（行业景气先行指标）
- `daily` — 一键日报（核心信号聚合）
- `industry` — 汽车行业景气数据（乘联会 CPCA）
- `buyer` — 买方视角（预测修正/基金重仓/风险收益）
- `insider_view` — 大股东视角（增持/减持决策框架）
- `backfill_dividend` — 回填历史股息率（自 1990 年开市以来）
- `dividend_rank` — 股息率榜单时长（占据高股息榜最久的股票）
### 历史与回填

- `backfill` — 回填上市以来全量数据（行情/公告/研报/财报）
- `history` — 上市以来统计（需先 backfill）
- `backfill_kline` — K 线增量更新（每日任务）
- `backfill_indicators` — 回填选股指标历史（增速/估值）
- `backfill_sgr` — 回填 SGR 持续增长率历史（1995 年报起）
### 报告与导出

- `export` — 导出复盘报告（Obsidian Markdown）
- `obsidian` — 管理独立 Obsidian 知识库
- `review` — 生成复盘报告（默认今天；--backfill 回填历史）
- `report` — 生成周/月/年复盘汇总报告
- `period` — 周期报告（日报/周报/月报，多视角）
### 数据与资讯

- `news` — 公告与研报（仅 A 股，自动入库）
- `financial` — 财报分析（A 股人民币 / 港股港元，东财接口）
- `ipo` — IPO 公司分析（近期新股列表 / 单只详情 / --report 报告）
- `ad` — A 股全栈数据（融合 a-stock-data）
- `arxiv` — arXiv 论文监测（公司署名单位）
- `hf` — HuggingFace 监测（模型 + 收录论文）
### 监控

- `monitor` — 持续监控（默认命令）
- `once` — 获取一次行情快照后退出
- `scan` — 全市场异动扫描（涨幅/跌幅/放量/换手/振幅榜）
- `events` — 事件日历提醒（解禁/分红除权/业绩预告）
- `fundflow` — 资金面监控（个股主力资金流 + 沪深港通）
- `holders` — 股东分析（十大股东 + 股东户数趋势）
- `litigation` — 诉讼监控（自选股重大诉讼披露）
- `gov` — 政府侧企业动态（中标/拿地/补助/税收优惠公告）
- `site` — 官方网站链接档案 + 官网公告监控
- `insider` — 增减持与回购监控（公告信号）
- `pledge` — 股权质押监控（巨潮）
- `rating` — 券商研报监控（含 EPS 预测）
- `lhb` — 龙虎榜监控（自选股上榜）
- `north` — 北向持股监控（个股持股明细）
- `block` — 大宗交易监控（折溢率）
- `radar` — 信号聚合雷达（多维度多空计分）
### 策略与交易

- `portfolio` — 组合定投回测（多标的按权重）
- `position` — 持仓管理与盈亏日报
- `strategy` — 低频策略：目标持仓 + 模拟交易
### 选股

- `screen` — A 股市场扫描选股（高股息率）

## 命令详情

### analyze

拉取个股历史数据并分析

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 600519 / 00700 / BTCUSDT |
| --market | 否 | ashare | 市场（默认 ashare） |
| --days | 否 | 250 | 回看K线数（默认 250） |
| --adjust | 否 | qfq | 复权方式（默认 qfq 前复权） |
| --period | 否 | daily | K 线周期：日线/周线/月线（默认 daily） |

### advice

规则化交易信号（结合实时行情）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 600519 / 00700 / BTCUSDT |
| --market | 否 | ashare | 市场（默认 ashare） |
| --days | 否 | 120 | 回看交易日数（默认 120） |

### indicator

技术指标（MACD/RSI/KDJ/BOLL）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 600519 / 00700 / BTCUSDT |
| --market | 否 | ashare | 市场（默认 ashare） |
| --days | 否 | 120 | 回看K线数（默认 120） |
| --period | 否 | daily | K 线周期：日线/周线/月线（默认 daily） |

### backtest

持有期回测（买入日/金额/持有交易日数）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594 / 01211 / BTCUSDT |
| --market | 否 | — | 市场（缺省按代码位数推断） |
| --buy-date | 否 |  | 买入日期 YYYY-MM-DD（默认最近交易日） |
| --amount | 否 | 100000.0 | 买入金额（元，默认 100000） |
| --hold-days | 否 | 60,120,250 | 持有交易日数，逗号分隔多档（默认 60,120,250） |
| --dca | 否 | — | 定投模式：每月首个交易日买入固定金额，持有统计 |
| --months | 否 | 60 | 定投回看月数（默认 60） |
| --detail | 否 | — | 定投模式显示逐笔明细 |
| --compare | 否 |  | 多标的定投对比，逗号分隔（如 002594,01211） |
| --chart | 否 | — | 生成单笔回测 K 线可视化 HTML（含买卖点标注） |

### verify

信号命中率验证（基于回填 K 线回测）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594 / 01211 |
| --market | 否 | — | 市场（缺省按代码位数推断） |
| --rule | 否 | — | 指定规则（缺省全部），如 up_break |
| --days | 否 | 500 | 回看交易日数（默认 500） |
| --forward | 否 | 5 | 触发后观察交易日数（默认 5） |
| --report | 否 | — | 生成命中率验证报告（HTML + Obsidian） |

### timing

择时买入提醒（收盘后扫描买点信号）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定标的代码（缺省扫描全部自选股） |
| --forward | 否 | 5 | 历史命中率观察交易日数（默认 5） |
| --report | 否 | — | 生成择时信号报告（HTML + Obsidian） |
| --push | 否 | — | 有信号时推送 webhook（需 ASHARE_MONITOR_WEBHOOK 环境变量） |
| --concentrated | 否 | — | 只保留筹码集中标的的信号（需联网查股东户数，A 股） |

### doctor

个股全方位体检（评分+汇总报告）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594 |
| --report | 否 | — | 生成体检报告（HTML + Obsidian） |

### profile

公司档案（工商信息 + 股权结构）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594 |
| --report | 否 | — | 生成公司档案报告（HTML + Obsidian） |

### check

标的资料完整性体检（股票/港股/数字货币）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 代码，如 002594 / BTCUSDT |
| --market | 否 | — | 市场（缺省按 watchlist/代码推断） |
| --report | 否 | — | 生成体检报告 |

### valuation

估值分位（PE/PB 历史百分位）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --years | 否 | 5 | 回看年数 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### sector

月度产销快报（行业景气先行指标）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### daily

一键日报（核心信号聚合）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### industry

汽车行业景气数据（乘联会 CPCA）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |
| --detail | 否 | — | 分析师版：细分市场/国别结构/碳酸锂价格 |

### buyer

买方视角（预测修正/基金重仓/风险收益）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### insider_view

大股东视角（增持/减持决策框架）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### backfill_dividend

回填历史股息率（自 1990 年开市以来）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码（缺省全部自选股） |
| --report | 否 | — | 生成报告 |

### dividend_rank

股息率榜单时长（占据高股息榜最久的股票）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --years | 否 | 15 | 统计年数（默认 15） |
| --min-yield | 否 | 3.0 | 上榜阈值 %（默认 3） |
| --top-k | 否 | 50 | 每年按排名取 TOP K（0=用阈值） |
| --sort | 否 | years | 排序：years 上榜年数 / cum-yield 累计股息率 |
| --report | 否 | — | 生成报告 |

### backfill

回填上市以来全量数据（行情/公告/研报/财报）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594 / 01211 |
| --market | 否 | — | 市场（缺省按代码位数推断） |
| --kline | 否 | — | 仅回填日 K |
| --news | 否 | — | 仅回填公告/研报 |
| --financial | 否 | — | 仅回填财报 |

### history

上市以来统计（需先 backfill）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594 / 01211 |
| --market | 否 | — | 市场（缺省按代码位数推断） |

### backfill_kline

K 线增量更新（每日任务）

无参数

### backfill_indicators

回填选股指标历史（增速/估值）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |

### backfill_sgr

回填 SGR 持续增长率历史（1995 年报起）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码（缺省全部自选股） |
| --report | 否 | — | 生成报告 |

### export

导出复盘报告（Obsidian Markdown）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --date | 否 | — | 复盘日期 YYYY-MM-DD，默认今天 |
| --obsidian | 否 | — | 导出 Markdown 到 Obsidian 库（需 config.obsidian.vault） |

### obsidian

管理独立 Obsidian 知识库

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| action | 是 | — | init=初始化库结构；index=重建首页索引 |
| --vault | 否 | — | vault 路径（默认取 config.obsidian.vault） |

### review

生成复盘报告（默认今天；--backfill 回填历史）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --date | 否 | — | 复盘日期 YYYY-MM-DD，默认今天 |
| --backfill | 否 | — | 回填历史复盘：起始日期 YYYY-MM-DD（用本地 klines 库，需先 backfill --kline） |
| --end | 否 | — | 回填结束日期 YYYY-MM-DD，默认昨天 |

### report

生成周/月/年复盘汇总报告

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --weekly | 否 | — | 周报（默认） |
| --monthly | 否 | — | 月报 |
| --yearly | 否 | — | 年报（近 365 天） |
| --date | 否 | — | 周期结束日期 YYYY-MM-DD，默认今天 |

### period

周期报告（日报/周报/月报，多视角）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --period | 否 | daily | 周期（默认 daily） |
| code | 是 |  | 指定代码 |
| --push | 否 | — | 推送 webhook |

### news

公告与研报（仅 A 股，自动入库）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 6 位证券代码，如 600519 |
| --days | 否 | 90 | 研报回看天数（默认 90） |
| --local | 否 | — | 仅读取数据库，不联网 |
| --watchlist | 否 | — | 批量采集全部 A 股自选股入库 |

### financial

财报分析（A 股人民币 / 港股港元，东财接口）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 600519 / 01211 |
| --periods | 否 | 6 | 回看报告期数（默认 6） |
| --market | 否 | ashare | 市场（默认 ashare） |

### ipo

IPO 公司分析（近期新股列表 / 单只详情 / --report 报告）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| keyword | 是 |  | 新股代码或名称（缺省显示列表） |
| --limit | 否 | 30 | 列表条数（默认 30） |
| --report | 否 | — | 生成 IPO 分析报告（HTML + Obsidian Markdown） |
| --history | 否 | — | 历史 IPO 发行分析（逗号分隔代码，如 002594,01211） |

### ad

A 股全栈数据（融合 a-stock-data）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| sub | 是 | — | quote/hot/lhb/unlock/margin 两融/block 大宗/fundflow 资金流120日 |
| codes | 是 |  | 代码（逗号分隔） |

### arxiv

arXiv 论文监测（公司署名单位）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 002594（需英文名映射） |
| --name | 否 |  | 公司英文名（缺省用内置映射，如 BYD） |
| --days | 否 | 14600 | 回看天数（默认 14600 ≈ 近 40 年，覆盖 arXiv 全历史） |
| --limit | 否 | 20 | 最多返回论文数（默认 20） |
| --report | 否 | — | 生成论文监测报告（HTML + Obsidian） |
| --push | 否 | — | 推送论文摘要 webhook |

### hf

HuggingFace 监测（模型 + 收录论文）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | 证券代码，如 00700（腾讯）/ 09988（阿里） |
| --org | 否 |  | HF 组织名（缺省用内置映射，如 Qwen / tencent） |
| --company | 否 |  | 论文检索公司名（缺省用内置英文名） |
| --limit | 否 | 10 | 最多返回条数（默认 10） |
| --report | 否 | — | 生成 HF 监测报告（HTML + Obsidian） |
| --push | 否 | — | 推送 HF 摘要 webhook |

### monitor

持续监控（默认命令）

无参数

### once

获取一次行情快照后退出

无参数

### scan

全市场异动扫描（涨幅/跌幅/放量/换手/振幅榜）

无参数

### events

事件日历提醒（解禁/分红除权/业绩预告）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定标的代码（缺省扫描全部自选股） |
| --days | 否 | 30 | 未来天数窗口（默认 30） |
| --report | 否 | — | 生成事件日历报告（HTML + Obsidian） |
| --push | 否 | — | 推送事件提醒 webhook |

### fundflow

资金面监控（个股主力资金流 + 沪深港通）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定标的代码（缺省扫描全部自选股） |
| --report | 否 | — | 生成资金面报告（HTML + Obsidian） |
| --push | 否 | — | 推送资金面概要 webhook |

### holders

股东分析（十大股东 + 股东户数趋势）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 | — | A 股代码，如 002594 |
| --report | 否 | — | 生成股东分析报告（HTML + Obsidian） |
| --push | 否 | — | 推送股东分析摘要 webhook |

### litigation

诉讼监控（自选股重大诉讼披露）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码（缺省扫描全部自选股） |
| --days | 否 | 365 | 回看天数（默认 365） |
| --report | 否 | — | 生成诉讼监控报告（HTML + Obsidian） |
| --push | 否 | — | 推送诉讼摘要 webhook |

### gov

政府侧企业动态（中标/拿地/补助/税收优惠公告）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码（缺省扫描全部自选股） |
| --days | 否 | 30 | 回看天数（默认 30） |
| --report | 否 | — | 生成政府动态报告（HTML + Obsidian） |
| --push | 否 | — | 推送政府动态摘要 webhook |

### site

官方网站链接档案 + 官网公告监控

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码（缺省全部自选股） |
| --report | 否 | — | 生成官网档案报告（HTML + Obsidian） |

### insider

增减持与回购监控（公告信号）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --days | 否 | 30 | 回看天数 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### pledge

股权质押监控（巨潮）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --days | 否 | 7 | 回看天数 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### rating

券商研报监控（含 EPS 预测）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --days | 否 | 30 | 回看天数 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### lhb

龙虎榜监控（自选股上榜）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --days | 否 | 10 | 回看天数 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### north

北向持股监控（个股持股明细）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### block

大宗交易监控（折溢率）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --days | 否 | 10 | 回看天数 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### radar

信号聚合雷达（多维度多空计分）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | 是 |  | 指定代码 |
| --report | 否 | — | 生成报告 |
| --push | 否 | — | 推送 webhook |

### portfolio

组合定投回测（多标的按权重）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| codes | 是 | — | 标的代码，逗号分隔（如 002594,01211） |
| --weights | 否 |  | 权重 %，逗号分隔（缺省等权，如 60,40） |
| --amount | 否 | 10000.0 | 每月定投总额（默认 10000） |
| --months | 否 | 60 | 回看月数（默认 60） |
| --hold-days | 否 | 250 | 持有交易日数（默认 250） |
| --report | 否 | — | 生成组合回测报告（HTML + Obsidian） |

### position

持仓管理与盈亏日报

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --live | 否 | — | 使用实时行情（缺省用本地收盘价） |
| --report | 否 | — | 生成持仓报告（HTML + Obsidian） |
| --push | 否 | — | 推送盈亏日报 webhook |

### strategy

低频策略：目标持仓 + 模拟交易

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| strategy | 是 | — | 策略：dividend/backtest/rebalance/status/orders 订单历史/risk/track/breaker/factor |
| --codes | 否 |  | backtest：标的列表（逗号分隔，默认自选股 A 股） |
| --start | 否 | — | backtest：起始日期 YYYY-MM-DD（默认全历史） |
| --factor-name | 否 | momentum | factor：因子名（内置 momentum/rsi/volatility 或 factors.local.yaml 自定义，默认 momentum） |
| --expr | 否 | — | factor：直接传表达式，如 (close/Ref(close,60)-1)*100（优先于 --factor-name） |
| --list | 否 | — | factor：列出全部可用因子表达式 |
| --forward | 否 | 20 | factor：未来收益天数 |
| --rebalance | 否 | — | backtest：静态 vs 周期再平衡对比（含成本） |
| --frequency | 否 | monthly | 再平衡频率（默认月度） |
| --cost | 否 | 5.0 | 单边交易成本 bp（佣金+印花税+滑点，默认 5） |
| --capital | 否 | 100000.0 | 资金（默认 10 万） |
| --top | 否 | 10 | 持仓数量 |
| --min-yield | 否 | 3.0 | 最低股息率 % |
| --paper | 否 | — | 执行模拟买入（paper trading） |
| --dry-run | 否 | — | 只算成交不落库（配合 --paper） |
| --commission | 否 | 0.0 | 佣金（万分比，A 股参考 2.5=万 2.5；默认 0 关闭） |
| --stamp | 否 | 0.0 | 印花税（卖出万分比，A 股参考 5=0.05%；默认 0） |

### screen

A 股市场扫描选股（高股息率）

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| --metric | 否 | dividend | 指标：dividend/sgr/margin/share/lowval/growth |
| --market | 否 | ashare | 市场（us 仅支持 momentum 动量） |
| --top | 否 | 60 | 返回 TOP N |
| --min-yield | 否 | 3.0 | 最低股息率 %（默认 3） |
| --min-mv | 否 | — | 最小总市值（亿） |
| --max-mv | 否 | — | 最大总市值（亿） |
| --report | 否 | — | 生成选股报告 |
